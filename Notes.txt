If you get Unable to load the EventMachine C extension; To use the pure-ruby reactor, requi 'em/pure_ruby' run the following:
  gem uninstall eventmachine
  gem install eventmachine --platform ruby


To build in production:
  bundle exec jekyll build JEKYLL_ENV=production